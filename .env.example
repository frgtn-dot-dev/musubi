# For best expirience, don't move lines around.
# This example should be build in a way, that you only need to change few things for working prod.

# DB
POSTGRES_USER=musubi
POSTGRES_DB=musubi
# Please change the DB password... o .o
POSTGRES_PASSWORD=change_for_random_long_password

# Leave as is. Updates automagically
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}

# This port will server as the access point for your backend.
API_SERVER_PORT=7531

# Environment can be "dev" for development or anything else for prod at this point... :)
ENVIRONMENT=dev

# BETTER_AUTH_URL must be ORIGIN ONLY — no "/api" suffix.
# The client (services/api.ts) and Better Auth both append "/api/..." themselves.
# Wrong:   https://musubi.frgtn.dev/api  → produces /api/api/... → 404
# Right:   https://musubi.frgtn.dev
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET=your_secret_here

# SMTP For password reset and other transactional emails
SMTP_HOST=smtp.sendlayer.net
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
FROM_EMAIL=sender@example.com
