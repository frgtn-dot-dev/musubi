---
title: Client
description: Reference for the Musubi React Native / Expo mobile app.
---

import { Aside } from '@astrojs/starlight/components';

The client is a React Native app built with Expo SDK 56. It runs on iOS and Android.

## Tech stack

| Concern | Library |
|---|---|
| Framework | Expo SDK 56 / React Native 0.85 |
| Routing | Expo Router — file-based, tab + stack navigation |
| State management | Zustand |
| Local cache | SQLite via Drizzle ORM + `expo-sqlite` |
| Calendar UI | Custom, in `components/cal/` (month → day/week, pinch zoom, drag-to-create/move). See [Custom calendar](#custom-calendar). |
| Recurrence / date logic | `@musubi/calendar` — logic-only package (RRULE expansion + date helpers), no UI |
| Animations | Reanimated v4 + Gesture Handler |
| Real-time | `react-native-sse` (Server-Sent Events) |
| Auth client | Better Auth |
| Fonts | Inter Tight, Noto Serif, Shippori Mincho B1 |

## File structure

```
apps/client/
├── app/
│   ├── _layout.tsx           # Root layout — fonts, migrations, theme, auth routing, splash
│   ├── (auth)/
│   │   ├── welcome.tsx       # Landing — server URL, Google sign-in, sign in / sign up
│   │   ├── sign-in.tsx       # Email + password sign-in
│   │   └── sign-up.tsx       # Registration
│   ├── (tabs)/
│   │   ├── _layout.tsx       # Tab navigator + initial data fetch
│   │   ├── index.tsx         # Main calendar view
│   │   ├── agenda.tsx        # Upcoming events list
│   │   ├── calendars.tsx     # Manage calendars, members, external connections
│   │   └── settings.tsx      # User settings (incl. theme)
│   └── invite/
│       └── [token].tsx       # Deep-link invite acceptance
├── components/
│   ├── cal/                  # Custom calendar engine — layout.ts (geometry + tuning),
│   │                         #   MonthView, TimelineView (day/week), ModeSwitch
│   ├── calendar/             # Calendar feature UI (event/calendar modals, filter bar,
│   │                         #   AddEventModal incl. docked composer, ReorderableCalendarList)
│   └── ui/                   # App-wide UI primitives — Tap, Btn, Empty
├── constants/
│   ├── colors.ts             # Named colour swatches (appColors array)
│   ├── theme.ts              # Palettes, fonts, styles, calendarTheme, applyTheme()
│   └── const.ts              # Japanese month/day name arrays
├── hooks/
│   ├── useRefreshData.ts     # Full refresh pipeline (sync → delta → cache → stores)
│   ├── useVisibleEvents.ts   # Filters + sorts events by active calendars
│   ├── useEventsStream.ts    # SSE connection for real-time updates
│   └── useModalAnimation.ts  # Shared bottom-sheet animation hook
├── lib/
│   ├── confirm.ts            # confirm() / chooseOption() — native dialogs
│   ├── haptics.ts            # Semantic haptic vocabulary
│   ├── liveEvent.ts          # liveEventDetail() — keep open modals fresh
│   ├── calendarOrder.ts      # sortCalendars() — user's saved calendar order
│   └── eventPermissions.ts   # canEditEvent() — mirrors the server's event edit rule
├── services/
│   ├── api.ts                # Typed HTTP client for all API endpoints
│   ├── eventsCache.ts        # SQLite event cache (Drizzle + expo-sqlite)
│   ├── db.ts                 # Local SQLite database instance
│   └── notifications.ts      # Local scheduled notifications (expo-notifications)
├── store/
│   ├── useEventsStore.ts     # Events state + CRUD
│   ├── useCalendarsStore.ts  # Calendars state, active filter, solo mode
│   └── useSettingsStore.ts   # User settings (view mode, week start, theme…)
└── contexts/
    └── ServerContext.tsx      # Server URL + auth client instance
```

## UI primitives

Everything interactive is built from a small set of primitives in `components/ui/` and `lib/`. **These are the way to build UI in this app** — don't reach for bare `Pressable`, `Alert.alert`, or ad-hoc haptic calls.

### `Tap` (`components/ui/Tap.tsx`)

Drop-in `Pressable` replacement with the app-wide press feel: quick opacity dim plus a subtle Reanimated spring scale on press-in. Props:

- `haptic` — name of a haptic from `lib/haptics.ts` to play on press-in. **Default `false`**: haptics are reserved for important actions, not every row.
- `scaleTo` — press-in scale, `0.97` by default; pass `1` to keep only the dim.

<Aside type="caution">
`Tap` wraps its `style` in an animated array, so it does **not** support the function-style `style={({ pressed }) => …}` form. Pass a plain style or array.
</Aside>

### `Btn` (`components/ui/Btn.tsx`)

The app button: theme variant, press feel, haptic, and busy state in one place.

- `variant` — `primary` (solid fill), `secondary` (outline), `destructive` (vermilion; plays the `warn` haptic instead of `tap`)
- `loading` — shows a spinner and blocks presses; wire it to your in-flight state

### `Empty` (`components/ui/Empty.tsx`)

Zen empty state: a single kanji (default 空), breathing room, one quiet line of text. Use it wherever a list can be empty.

### `confirm()` / `chooseOption()` (`lib/confirm.ts`)

The one way to ask "are you sure?". `confirm()` shows the native `ActionSheetIOS` on iOS and `Alert` on Android, leading with a warning haptic. `chooseOption()` is the multi-choice variant — e.g. the recurring-event delete scope ("this / following / all").

### Haptics (`lib/haptics.ts`)

Semantic vocabulary so callsites read as intent: `tap` (light tick), `select` (**intentionally a no-op** — firing on every pill tap proved annoying, kept so callsites stay semantic), `thump` (medium — long-press, drag thresholds), `success`, `warn`. Policy: haptics only on important actions (primary/destructive buttons, FABs, confirms), not every touch.

## Theming

`constants/theme.ts` defines two palettes: **dark** (sumi ink on night) and **light** (ink on washi paper). The exported `colors`, `styles`, and `calendarTheme` are **mutable singletons** — `applyTheme(scheme)` swaps their contents in place with `Object.assign`, no context/provider plumbing.

The root layout resolves the effective scheme from the user's `theme` setting (`system` | `dark` | `light`, with `system` following `useColorScheme()`), calls `applyTheme(scheme)`, and remounts the whole tree via `key={scheme}` so every component re-reads the swapped tokens.

<Aside type="danger">
Three pitfalls that silently break theme switching:

1. **Never reference `colors.*` in module-level `StyleSheet.create` or module-level constants.** Module scope is evaluated once — the values go stale after a theme switch. Apply colours inline at render time (or inside a function called per render).
2. **`applyTheme` must be called as a plain statement in the component body, NOT inside `useMemo`.** The React Compiler assumes memo callbacks are pure and eliminates ones whose result is unused — which silently drops the side effect. It's idempotent, so calling it every render is fine.
3. **Never read `colors` inside a Reanimated worklet** (`useAnimatedStyle`, `useDerivedValue`, …). Reanimated serializes the captured `colors` object to the UI thread and freezes it, so `applyTheme`'s in-place `Object.assign` is rejected (you'll see `[Worklets] Tried to modify key 'bg'…` warnings) and the palette never swaps. Keep colour in the plain style: `style={[animatedStyle, { backgroundColor: colors.x }]}`.
</Aside>

### Token roles

| Token | Role |
|---|---|
| `bg` … `bg3` | Backgrounds, from screen base to elevated pills/inputs |
| `line` … `line3` | Hairlines and borders, increasing contrast |
| `fg` … `fg4` | Text, from primary to placeholder |
| `accent` | Vermilion — highlights, errors, now-indicator |
| `fill` / `onFill` | Solid active fills (pills, primary buttons) and the text/icon sitting on them |

Never hardcode hex values in components — always read from `colors`.

## Data flow

### Stores are API-first

Three Zustand stores (`useEventsStore`, `useCalendarsStore`, `useSettingsStore`) hold all server state. Mutating actions call the API **first** and only write to state and the SQLite cache after the call succeeds. Each API-backed action has a `local*` twin (`localAddEvent`, `localRemoveCalendar`, …) used by the SSE stream to apply changes that already happened on the server. The one deliberate exception is **drag-to-move** on the calendar (see [Custom calendar](#custom-calendar)): it applies locally first for a responsive drop and reverts if the request fails.

### Refresh pipeline (`hooks/useRefreshData.ts`)

A full refresh runs, in order:

1. **Provider sync trigger** — pokes the server to sync connected external providers first, so freshly imported changes land in the delta below (best-effort, no-op when nothing is connected).
2. **Delta fetch** — `GET /api/v1/events?since=<lastSync>` returns only events changed since the stored cursor, plus tombstone `deletedIds`. No valid cursor → full sync, which replaces the entire cache.
3. **Cache write** — upsert changed events and delete tombstoned ones in the SQLite cache (`services/eventsCache.ts`), then store the returned `serverTime` as the next cursor.
4. **Membership reconcile** — an offline kick sends no SSE and the delta can't tombstone events you merely lost access to, so events are re-checked against current calendar membership: links to calendars you're no longer in are stripped, and events left with none are purged.

### Real-time updates (`hooks/useEventsStream.ts`)

A persistent SSE connection to `GET /api/v1/stream` receives `event_created`, `event_updated`, `event_removed`, `calendar_updated`, and `calendar_removed` messages and applies them via the stores' `local*` actions. `calendar_removed` also calls `localRemoveCalendarEvents` to purge that calendar's events from memory and cache.

### Live detail modals (`lib/liveEvent.ts`)

An open detail modal holds a snapshot; SSE keeps the store fresh. `liveEventDetail(events, detail)` re-reads the live row so remote edits show without reopening — for recurring events it keeps the tapped occurrence's start/end, since the store row carries the series master's times.

## Modal pattern

Bottom sheets share `useModalAnimation(visible, onClose)`: critically-damped spring physics (one spec for enter, release, and dismiss), a pan gesture that follows the finger downward and rubber-bands upward, and dismissal past a distance or velocity threshold.

<Aside type="caution">
Closing happens on a plain `setTimeout`, **not** an animation completion callback — an interrupted animation can drop its callback, leaving an invisible "ghost" modal that blocks all touches.
</Aside>

## Custom calendar

The calendar UI is hand-built in `components/cal/` — a small engine the screens (`app/(tabs)/index.tsx`, `CalendarDetailModal`) drive. There is no third-party calendar widget.

| File | Responsibility |
|---|---|
| `layout.ts` | All geometry + date math + tuning constants in one place — `HOUR_H`, `GUTTER`, snap/hold thresholds, zoom bounds, `minutesToY`, `bucketByDay`, `daySegments` (overlap columns), `allDaySpans`, `monthGrid`. Change timing/spacing here, not in components. |
| `MonthView.tsx` | Paged month grid (6×7). Tapping a day zooms into it; events are chips, not tappable. |
| `TimelineView.tsx` | Day and week timeline: hour grid, event blocks, all-day strip, the drag-to-create ghost, and all the gestures. |
| `ModeSwitch.tsx` | The day / week / month dropdown (Musubi-style, not the OS menu). |

**Coordinate model.** Vertical position is minutes → pixels through `hourH` (pixels per hour). `hourH` is a **Reanimated shared value** so a pinch can rescale the whole timeline on the UI thread with no React re-render — hour lines, the now-indicator, and every event block derive their `top`/`height` from it (blocks via `useAnimatedReaction`). Horizontal position is the day column; `minutesToY`/`daySegments` handle the rest.

**Gestures** (all Reanimated + Gesture Handler):

- **Pinch to zoom** sits on the *outer* page view and is declared `simultaneousWithExternalGesture` with the ScrollView, so one-finger scroll and event taps keep working while a two-finger pinch rescales `hourH`. It keeps the point under the fingers fixed by scrolling to compensate.
- **Drag-to-create** — hold an empty slot, then drag to size the range. The ghost snaps to 15-minute steps and holds each step; its corner dots resize the edges, the middle moves it.
- **Drag-to-move** an existing block — hold to pick up, drag to reschedule (vertical = time, horizontal = whole-day steps). Recurring or read-only events can't move and wear a lock icon. This is the app's one **optimistic** path: it writes locally immediately, calls `PUT /api/v1/events`, and reverts on failure.
- **Tap** a block opens its detail. Because an ancestor gesture detector would otherwise swallow a `Pressable`'s release, the block is a plain `Tap` whose press is preserved by keeping the pinch on the outer view (two-finger only).

<Aside type="note">
**Flicker-free drop.** A block's position lives in shared values, never React layout. On drop the raw drag is folded into the base coordinates while the transforms zero out in the *same* UI-thread frame — identical pixels before and after — then a layout effect glides to the snapped slot. Same trick powers the drag-reorder in `ReorderableCalendarList`.
</Aside>

**Docked composer.** New events use `AddEventModal` in `docked` mode: a sheet pinned to the bottom that peeks with a title field + Save, expands when you type or pull it up (a backdrop then blocks the calendar), and reads its times live from the drag/tap draft on the grid.

## Recurrence expansion

Recurring events are stored as a single row with an RRULE string (optionally multi-line iCal with `EXDATE` exception lines) and expanded on the client by `expandRecurringEvents()` from `@musubi/calendar` for the visible date range. Parsed rules are cached per `(rrule, dtstart)` pair. Occurrence ids are synthetic — `` `<originalId>_<startTimestamp>` `` — stable across renders so list keys don't thrash.

## Adding a screen

1. Create a file in the appropriate `app/` directory
2. Export a default React component as the screen
3. If it's a new tab, add it to `app/(tabs)/_layout.tsx`
4. Use `styles.screen` as the root container style — it handles background colour and flex
